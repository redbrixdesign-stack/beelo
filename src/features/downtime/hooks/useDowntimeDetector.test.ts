import { describe, it, expect } from 'vitest'

describe('useDowntimeDetector', () => {
  it('returns no prompt when no advisor', async () => {
    expect(true).toBe(true)
  })

  it('returns no prompt when not synced', async () => {
    expect(true).toBe(true)
  })
})